[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: send every unmatched request to index.html so client-side
# routing (TanStack Router) handles deep links and page refreshes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
